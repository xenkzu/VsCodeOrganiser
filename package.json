{
  "name": "nette",
  "displayName": "Nette",
  "description": "Automatically organizes DSA practice files into topic folders based on code content: no naming conventions required.",
  "version": "1.0.0",
  "publisher": "YashKaul",
  "icon": "assets/icon.png",
  "galleryBanner": {
    "color": "#1e1e2e",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Formatters",
    "Education"
  ],
  "keywords": [
    "dsa",
    "algorithms",
    "data structures",
    "organizer",
    "workspace",
    "leetcode",
    "competitive programming",
    "file organization",
    "productivity",
    "nette"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_USERNAME/nette"
  },
  "bugs": {
    "url": "https://github.com/YOUR_USERNAME/nette/issues"
  },
  "homepage": "https://github.com/YOUR_USERNAME/nette#readme",
  "license": "MIT",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "nette.organize",
        "title": "Nette: Organize Workspace"
      },
      {
        "command": "nette.toggleEnabled",
        "title": "Nette: Toggle Enabled"
      },
      {
        "command": "nette.undoLast",
        "title": "Nette: Undo Last Move"
      }
    ],
    "configuration": {
      "title": "Nette Organizer",
      "properties": {
        "nette.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable Nette Organizer."
        },
        "nette.rootDir": {
          "type": "string",
          "default": "DSA",
          "description": "Root folder for organized files, relative to workspace. Set to '.' or '' to place files directly in the workspace root with no wrapper folder."
        },
        "nette.confidenceThreshold": {
          "type": "number",
          "default": 0.75,
          "description": "Minimum confidence required for automatic move."
        },
        "nette.aiEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable AI fallback classification using Groq when heuristic confidence is low."
        },
        "nette.groqApiKey": {
          "type": "string",
          "default": "",
          "description": "Groq API key for AI-assisted classification. Get one free at console.groq.com. Stored in VS Code settings — use workspace settings to avoid committing to git."
        },
        "nette.debounceMs": {
          "type": "number",
          "default": 300,
          "description": "Debounce time for file system events in milliseconds."
        },
        "nette.autoNumber": {
          "type": "boolean",
          "default": true,
          "description": "Prefix moved files with the smallest available number in the destination folder (e.g. 0_filename.py)"
        },
        "nette.manualSaveOnly": {
          "type": "boolean",
          "default": true,
          "description": "Only organize files on manual save (Ctrl+S). When enabled, auto-save (on focus change or after delay) will not trigger the organizer."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "package": "vsce package --no-dependencies",
    "publish": "vsce publish"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/vscode": "^1.85.0",
    "@vscode/test-electron": "^2.3.8",
    "@vscode/vsce": "^2.22.0",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}